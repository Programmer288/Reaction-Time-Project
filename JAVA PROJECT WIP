Since i am a simple beginner at Java, i decided to make an introductory museum-like game. 
Many students solutions will naturally look similar, since there are many straightforward ways to implement this assignment. 
I used a tutorial for a text-adventure based game; then created my own original idea that can benefit from the tutorial code. 
The idea and passion are all mine. Sources: https://www.youtube.com/watch?v=WZUs-gI76WY
import java.util.ArrayList
import java.util.List
import java.util.Map;
import java.util.Scanner; 
System.out.println("Welcome to my musuem!")
System.out.println("The objective of this game is to learn about which technological fact intriguees you the most!")
System.out.println("Press the SPACE key to start.")

public class consoleStart {
public static void main(String[] args) {
Scanner scanner = new Scanner(System.in);
String input = scanner.nextLine();
if (input equals = " ")) {
System.out.println(SPACE CLICKED!)

//tutorial
   while (true) {
       String input = scanner.nextLine();
       if (input.equalsIgnoreCase("quit")) {
           System.out.println("Thank you for playing!")
           break;
       }
   processCommand(input);
   }
   //tutorail
public class Room {
    private string description;
    private Man<String, Room> exits;
    
    public Room(String description) {
        this.description = description;
        this.exits = new HashMap<>();
    }
    public void setExit(String direction, Room room) {
     exits.put(direction, room);
    }
public Room getExit(String direction) {
    return exits.get(direction);
}
public String getDescription() {
    return description;
}
//47 is from the tutorial. However, i used this code strucure to apply this logic to other rooms
Room startRoom = new Room("You are in a very dark dungeon.. what secrets hide in here?");
Room RoboticsRoom = new Room("The door to the west leads to a beatiful garden.");
Room AIRoom = new Room("The door to the west leads to a beatiful garden.");
//Tutorial code, however i used my idea.
startRoom.setExit("Robotics", RoboticsRoom)
roboticsRoom.setExit("AI", AIRoom)
//tutorial, but for my museum rooms
case "learn Robotics":
Room nextRoom = currentRoom.getExit("Robotics");
if (nextRoom != null) {
currentRoom = nextRoom;
System.out.print.ln(currentRoom.getDescription)

case "learn AI":
Room nextRoom = currentRoom.getExit("AI");
if (nextRoom != null) {
currentRoom = nextRoom;
System.out.print.ln(currentRoom.getDescription)

case inspect
if currentRoom.getItem().eqauls
System.out.println

case inspect
if currentRoom.getItem().eqauls
System.out.println

case inspect
if currentRoom.getItem().eqauls
System.out.println

case inspect
if currentRoom.getItem().eqauls
System.out.println
